import { StyleSheet, Text, TextInput, View } from 'react-native'
import React from 'react'

const TextInputOne = () => {
    return (
        <TextInput style={{width:360, height:40, paddingHorizontal:20, backgroundColor:"rgba(218, 218, 250, 0.1)", borderColor:"#7C7CFC", borderWidth:1, borderRadius:20}} />
    )
}

const styles = StyleSheet.create({})

export default TextInputOne